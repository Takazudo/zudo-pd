LM2596 feedback-divider value-sizing — command log
date: 2026-06-17
host: macOS (darwin 25.5.0, arm64)
worktree: worktrees/spice-loop  branch: ai-circuit-design/spice-loop

=====================================================================
1. SIMULATOR DISCOVERY (no rabbit-holing)
=====================================================================

$ command -v ngspice
  (no output) ; exit 1   -> ngspice NOT on PATH

$ ls -d /Applications/KiCad/KiCad.app
  /Applications/KiCad/KiCad.app/   -> KiCad is installed

$ find /Applications/KiCad/KiCad.app -name 'ngspice*'
  /Applications/KiCad/KiCad.app/Contents/PlugIns/sim/ngspice   (a DIRECTORY of .cm code models, not a binary)

$ ls /Applications/KiCad/KiCad.app/Contents/PlugIns/sim/ngspice
  analog.cm digital.cm ivlng.so ivlng.vpi spice2poly.cm table.cm xtradev.cm xtraevt.cm

$ find /Applications/KiCad/KiCad.app -type f \( -name ngspice -o -name ngspice_con \)
  (no output)   -> KiCad bundles ngspice ONLY as a shared library, not a CLI exe

$ find /Applications/KiCad/KiCad.app -name 'libngspice*'
  .../Contents/Frameworks/libngspice.0.dylib
  .../Contents/PlugIns/sim/libngspice.0.dylib
  .../Contents/PlugIns/sim/libngspice.dylib
  -> usable only from KiCad's GUI simulator (libngspice), no command-line entry point

$ /Applications/KiCad/KiCad.app/Contents/MacOS/kicad-cli --help | grep -iE 'sim|spice'
  (no output)   -> kicad-cli has no batch sim subcommand

CONCLUSION: No invokable command-line SPICE simulator is available.

=====================================================================
2. SINGLE, NON-INTERACTIVE, TIMEOUT-LIMITED INSTALL ATTEMPT
=====================================================================

$ gtimeout 180 brew install ngspice
  ... ✔︎ Bottle Manifest libngspice (46)   [downloads started]
  ... ✔︎ Bottle libomp / libpng / freetype / xorgproto / libxau / libxdmcp
  Error: SIGTERM            <- killed by the 180s timeout mid-download
  (download_queue.rb sleep)

$ command -v ngspice
  ngspice still not on PATH

DECISION (per issue guardrails): one attempt only, it timed out, do NOT retry.
Deliver the deck + an algebraic (hand-computed) read-back instead.

=====================================================================
3. HAND-COMPUTED READ-BACK (stand-in for the .op result)
=====================================================================
Formula the deck evaluates:  Vout = Vref * (1 + R3/R4),  Vref = 1.23 V

$ python3  (algebra only — no SPICE)
  As-designed +7.5V rail:
    R3=5.1k R4=1k -> Vout = 1.23*(1+5.1/1) = 7.5030 V   (target 7.5 V  OK)
    FB node at Vout=7.5 -> 7.5*1/(5.1+1) = 1.2295 V ~= Vref 1.23  OK

  Loop "adjust & re-check" — retarget to +13.5V rail (R4 held at 1k):
    needed R3 = R4*(Vout/Vref - 1) = 1k*(13.5/1.23 - 1) = 9.976 k
    R3=9.1k  -> 12.423 V
    R3=10k   -> 13.530 V   (closest E24, +0.22%)
    R3=11k   -> 14.760 V

  These are the exact node voltages the .op / sweep deck would print.

=====================================================================
4. TO RUN THE DECK ONCE A SIMULATOR IS PRESENT
=====================================================================
  brew install ngspice          # one-time, ~1-2 min on a normal connection
  ngspice -b lm2596-divider.cir            # batch: prints the .op results
  ngspice -b lm2596-divider-sweep.cir      # R3 sweep -> rail vs R3 table

Expected lm2596-divider.cir output (matches the hand calc above):
  v(vout_a) = 7.503   v(fb_a) = 1.2295   fb_err_a ~ 0
  v(vout_b) = 7.503   v(fb_b) = 1.23
  ratio = 6.1   (== 1 + 5.1/1)
